
export type Transaction = {
  id: string;
  type: 'income' | 'expense';
  category: string;
  amount: number;
  date: Date;
  description: string;
};

export type ReferredUser = {
  id: string;
  name: string;
  email: string;
  signupDate: Date;
  status: 'Active' | 'Pending';
  referredBy: string | null; // null for direct referrals of the main user
  earnings: number; // Represents revenue generated by this user
};

export type Currency = 'USD' | 'EUR' | 'JPY' | 'GBP' | 'PHP';

export type Category = {
  value: string;
  label: string;
  icon: React.ElementType;
};

export type SubscriptionFeature = {
  text: string;
  earningCap?: number;
};

export type SubscriptionTier = {
  name: string;
  price: number;
  features: SubscriptionFeature[];
  requiredReferrals: number;
  earningCap: number;
  borderColor: string;
  gradientFrom: string;
  gradientTo: string;
  textColor: string;
};

export type ReferralMilestone = {
  name: string;
  requiredReferrals: number;
};
